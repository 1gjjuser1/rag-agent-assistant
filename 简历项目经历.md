# 简历项目经历（可直接使用/按需调整）

> 使用说明：把【公司/部门】替换成实际名称；量化数字已从代码中核实（41 项测试等）。
> 如投递的岗位偏"算法/检索"可多强调混合检索与防幻觉，偏"后端/工程"可多强调存储设计与并发入库。

---

## 项目名称

**企业级智能文档问答系统（RAG + Function Calling Agent）**

## 技术栈

Python 3.10 · Streamlit · LangChain · Qwen / DashScope · Chroma · SQLite · jieba · rank-bm25 · PyMuPDF / RapidOCR · pytest

## 项目描述（一句话）

从 0 到 1 实现企业知识库多文档问答原型：以"向量 + BM25 混合检索 + RRF/MMR 融合排序"为底座，接入 Function Calling 多工具 Agent 与流式对话，检索无结果时明确拒答而非编造，回答逐条标注可溯源引用。

## 项目经历（简历正文，可按条删减）

**企业级智能文档问答系统（RAG + Agent）**　|　独立开发　|　Python / LangChain / Qwen / Chroma / SQLite / Streamlit

- 设计并实现多知识库文档问答系统：支持知识库独立创建/切换/删除、文档增量入库、同名文件自动版本化与软删除；元数据用 SQLite（WAL 模式 + 外键约束）落库，向量按知识库分 Chroma collection，物理文件按版本归档，数据层与检索层解耦、可平滑迁移 PostgreSQL / 分布式向量库。
- 实现混合检索与防幻觉：jieba 中文分词 + BM25 词面检索与向量语义检索双通道，RRF 无参融合排序、MMR 多样性重排、相关性阈值门控；针对简历/人名类文档实现来源限定检索（命中文件名或人物短名时自动锁定单一文档），有效防止跨文档"串台"；回答 prompt 强制逐条标注 [来源: 文件, 页码/段落]，未检索到相关内容时明确拒答。
- 基于 Qwen Function Calling 实现可插拔 ReAct Agent：LLM 自主选择知识库检索 / 天气 / 联网搜索工具，多步循环执行、工具异常回填重试、结果 2000 字符截断护栏；工具注册表设计使新增工具仅需"注册函数 + JSON Schema"；以事件流（tool_start / tool_end / token）驱动前端实时渲染工具调用轨迹。
- 优化多轮对话与上下文管理：追问题自动改写为独立问题（带长度护栏与失败回退），对话历史与检索片段按 token 预算从后往前截断，防止长上下文注意力稀释；Embedding 批量上限按 API 约束钳制，索引配置签名变化时自动全量重建。
- 工程化与质量保障：后台线程异步入库不阻塞聊天；PDF 文本质量检测 + 本地 RapidOCR 兜底处理乱码/扫描件；全链路 41 项离线测试（mock LLM 与 Embedding，覆盖混合检索、增量入库、版本管理、Agent 工具循环、端到端冒烟），ruff 静态检查通过。

## 可选补充（面试时可展开的点）

- 已知限制与演进规划：当前为单机单用户原型，计划升级 FastAPI 服务层、用户/租户与鉴权、任务队列化入库、RAGAS 评测体系。

