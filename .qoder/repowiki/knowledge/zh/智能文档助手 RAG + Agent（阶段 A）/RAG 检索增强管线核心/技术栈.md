ChromaDB 作为向量库（cosine HNSW）、SQLite WAL 模式作元数据存储、LangChain 的 `PyPDFLoader`/`Docx2txtLoader`/`TextLoader` 与 `RecursiveCharacterTextSplitter` 做解析切分、可选 PyMuPDF + RapidOCR 对扫描 PDF 做本地 OCR、BM25Index + 自定义 `reciprocal_rank_fusion`/`mmr_rerank` 实现混合检索。