- 通过 `conftest.py` 中的 `pipeline` 与 `agent` fixture 复用已注入 `FakeLLMClient` 的 RAGPipeline 与 ReActAgent 实例，测试用例仅声明参数即可获取。
- 对外部 LLM 调用一律用 `FakeLLMClient` 的 `script` 列表驱动，按顺序弹出 `FakeMessage`（可携带 `tool_calls`），从而精确控制 function calling 流程。
- 知识库与文档操作统一通过 `tests.helpers.upload_text` 与 `ingest_all` 封装，避免在断言中重复写入文本与调用 ingest。
- 所有持久化测试使用 pytest 的 `tmp_path` fixture 构造独立 `AppConfig`，保证 SQLite 与 Chroma 目录互不干扰。
- 对第三方网络依赖（如天气 API）使用 `monkeypatch.setattr` 就地替换 `urlopen`，返回自定义 `FakeResponse`，保持测试完全离线。
- 断言采用“正向结果 + 边界行为”双断言模式，例如既检查 `indexed` 列表又校验 `kb_stats` 计数或向量集合大小。